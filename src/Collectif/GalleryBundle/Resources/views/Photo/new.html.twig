{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Photo creation</h1>

    <form action="{{ path('photo_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('photo') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
