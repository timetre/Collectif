{% extends 'CollectifAdminBundle::layout.html.twig' %}

{% block title %}Collectif CONFLUENCE{% endblock %}

{% block content %}

<h1>Liste des cat&eacute;gories</h1>

<div class="header-buttons">
	<a class="btn btn-success right" href="{{ path('collectif_categories_create') }}"><i class="icon-plus-sign icon-white"></i>Ajouter une catégorie</a>
</div>

<table class="table table-hover">
	<tr>
		<th>Editer</th>
		<th>Titre</th>
		<th>Supprimer</th>
	</tr>
{% for c in categories %}
	<tr>
<td><a class="btn" href="{{ path('collectif_categories_edit', { 'id': c.id }) }}"><i class="icon-edit"></i></a></td>
<td>{{ c.titre }}</td>
<td><a onclick="if(!confirm('Voulez-vous vraiment supprimer cette catégorie ?')) { return false };" class="btn btn-danger" href="{{ path('collectif_categories_delete', { 'id': c.id }) }}"><i class="icon-white icon-trash"></i></a></td>
</tr>
{% else %}
	<tr><td>Aucune catégorie n’a été trouvée.</td></tr>
{% endfor %}
</table>

{% endblock %}